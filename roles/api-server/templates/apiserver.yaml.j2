apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
    - names:
      - "{{ api_server_dns }}"
      servingCertificate:
        name: {{ api_tls_secret_name }}
